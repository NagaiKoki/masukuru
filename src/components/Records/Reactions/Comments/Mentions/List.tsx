import React from 'react'
import styled from 'styled-components'
// import types
import { GroupUserType } from '../../../../../types/Group'

type PropsType = {
  groupUsers: GroupUserType[]
}

const CommentMentionList = (props: PropsType) => {
  const { groupUsers } = props

  return (
    <Wrapper>
      
    </Wrapper>
  )
}

const Wrapper = styled.View``