import React from 'react'
import styled from 'styled-components'
// import types
import { GroupUserType } from '../../../../../types/Group'

type PropsType = {
  groupUser: GroupUserType
}

const CommentMentionItem = (props: PropsType) => {
  const { groupUser } = props
}