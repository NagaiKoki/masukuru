import React from 'react'
import styled from 'styled-components'
// import components
import { TextInput } from './TextInput'

type MentionPropsType = {
  text: string
  onChange: (value: string) => void
}

export const Mention = (props: MentionPropsType) => {
  return (
    <TextInput 
    />
  )
}